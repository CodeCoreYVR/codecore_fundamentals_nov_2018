/**
 * Interpolation is when we have the ability to
 * run code within a string
 */

console.log( "Hi my name is arturo" );
console.log( 'I am a full stack developer' );
console.log( `and i'm currently at codecore` );
console.log( `2 + 2` );

/**
 * Type casting is when we transform a value
 * that has a specific data type, to a different
 * data type.
 */

console.log("234");
console.log(234);


console.log(2 + '2' ); // 22
console.log(2 + parseInt('2') ); // 2

// arithmetic operators

console.log(2 * 2) // multiplication
console.log(2 / 2) // division
console.log(2 % 2) // modulus
console.log(2 - 2) // subtraction

// comparisson operators
// == != < > <= >=

// comparisson operators
// == != < > <= >=

// double equals operator will
// only compare the value

2 == 2 // true
2 != 2 // false
2 == '2' // true

// STRICT COMPARASON OPERATORS
// triple equals operator will
// compare the value and its data type
// === !===

2 === '2' // false
2 !== '2' // true

// Logical operators
// && (and) || (or)

// with the && (AND) operator
// all elements, must be true
// for the entire statement
// to be true
true && true // true
true && false // false
false && true // false
false && false // false


// with the || (OR) operator
// at least one element
// must be true
// for the entire statement
// to be true
true || true // true
false || true // true
true || false // true
false || false // false



2 == 2 && 2 == 3 // false
2 === parseInt('2'); // true


parseInt('A')
'A'.charCodeAt() // 65 - 01000001






